version: '2.3'
services:
  frontend:
    build: ./frontend
    volumes:
      - ./frontend:/app
    env_file: ./frontend/.env
    ports:
      - "8080:3000"
    # depends_on:
    #   - backend
    tty: true
    stdin_open: true
